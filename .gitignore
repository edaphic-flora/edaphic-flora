.Rproj.user
.Rhistory
.RData
.Ruserdata
.Rdata
.httr-oauth
.DS_Store
.quarto

# Environment files with secrets (use .renviron.example as template)
.Renviron
app/.Renviron

# Claude Code files (local development only)
.claude/
# CLAUDE.md - now tracked for documentation purposes
Claude To-do.txt

# Archived reference folder
for_claude_EdaphicFlora/

# Branding assets (local only)
branding/

# Test files with personal data
test_soil_reports/

# Large cache files (~67MB, 10K+ JSON files)
# These are already loaded into the database; keep locally but don't commit
app/data/cache/

# Windows artifacts
nul
